```
APPLIED: C1,C2,C3,C4,I1,I2,I3,I4,I5,I6,I7,I8,I9,N1,N2
SKIPPED: plan/ledger metric disagreements + bundled plans//executions//ledger/*.jsonl (harness-generated artifacts, repo convention commits them, out of code-review scope); --token-reduce on `intent` subcommand (not in Phase 1 plan scope); shipping a real mitm_addon.py (Phase 2 — Phase 1 now fails loudly via FileNotFoundError as required by C3)
TESTS: 111 passed, 44 subtests passed in 39.81s
LINT: ruff check clean; ruff format --check clean (17 files already formatted); prettier clean (docs/sandbox/*.md, README.md)
COMMIT: 6e25912
PUSHED: yes
```

Notes on implementation decisions and empirical proof:

- **C1** `cli.py:309` now mounts only `~/.holon/proxy-cache:/home/mitmproxy/.holon/proxy-cache:ro`; the wholesale `~/.holon` mount (CA key + `sessions/antigravity`) is gone.
- **C2** `_generate_fallback_cert` deleted; `shutil.which("openssl")` probed first, `CalledProcessError`/`TimeoutExpired` translated to `RuntimeError` with stderr, `timeout=60`, key pre-created `0o600` + `chmod`, `_assert_valid_cert` on both fresh and reuse paths. Proved live: `cert parseable: True`, `key mode: 0o600`, poisoned cache → `RuntimeError: ... is not a parseable X.509 certificate ... Delete <dir>`, missing openssl → hint with `brew install openssl` / `apt-get install openssl`.
- **C3** addon `isfile` guard raises `FileNotFoundError` with the expected path; non-zero spawn raises `RuntimeError` with stderr + "Re-run without --token-reduce"; `time.sleep` replaced by `_wait_for_proxy` TCP retry loop (0.5s / 15s). Readiness probing needs host reachability, so the sidecar publishes a **loopback-only** port (`-p 127.0.0.1::8080`, resolved via `docker port`) — validated end-to-end against a real container: `published loopback port: 49462`, `readiness probe: connected`, flags accepted (`268435456 500000000 no json-file`, mount `RW=false`), teardown removed container + network.
- **C4/I6** verified live: `HTTP_PROXY`/`HTTPS_PROXY` set with `token_reduce=False` → `([], {})`; `HOLON_TOKEN_REDUCE=1` + unreachable proxy → `([], {})` with `ERROR ... continues with DIRECT egress`; `--token-reduce` with missing addon → `([], {})` + actionable error. Reachable proxy → CA `:ro` mount + proxy envs.
- **I2/I5** per-run names `holon-proxy-<pid>-<uuid8>` / `holon-net-<pid>-<uuid8>`, ownership tracked in `_SidecarState`, `finally: teardown_token_reduction_proxy()` removes only what this run created (no-op otherwise; "already exists" ⇒ network not owned, not removed).
- **I4** `role_dispatcher.sh` runs guarded `update-ca-certificates || true` before role dispatch (`bash -n` clean).
- Untouched: `find_github_token`'s `except Exception` (not a token-reduction path). The stray untracked `apps/sandbox-executor/uv.lock` (python-3.14 artifact from an earlier `uv` run, pre-existing) was deliberately **not** committed.
