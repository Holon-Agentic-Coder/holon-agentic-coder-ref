All fixes implemented, verified empirically, committed and pushed.

```
APPLIED: C5, C6, C7, I10, I11, I12, I13, I14, I15, I16, I17, NITs (main-vs-reuse report, probe-before-generate, image-pull log, CURL_CA_BUNDLE comment, module-scoped stdlib patches)
SKIPPED: mitm_addon.py / making --token-reduce functional (Phase 2 scope; preflight still fails loudly and docs now say experimental) | digest-pinning mitmproxy/mitmproxy:12.2.3 (needs a registry-promotion workflow outside this PR) | plans/, executions/, ledger/*.jsonl + metric drift (harness-generated artifacts)
TESTS: 120 passed, 44 subtests passed in 40.04s (test_token_reduction.py: 35 passed, up from 26; no test weakened or deleted)
LINT: ruff check "All checks passed!" | ruff format --check "17 files already formatted" | prettier "All matched files use Prettier code style!" | bash -n clean
COMMIT: ffa705f (see ANOMALY — content spans 84380d6 + ffa705f)
PUSHED: yes (fast-forward 84380d6..ffa705f on the PR head branch; never main/develop)
```

**CRYPTO_PROOF (C5 + I10, verbatim command from the task):**
```
keyUsage present: True | certSign: True | CA:TRUE: True
checkend 30d: True
key mode: 0o600
```
**I10 expiry/regeneration proof:**
```
cached 5-day CA -> -noout exit (stale-looking-ok): 0 | expiring within 30d: True
after generate_root_ca: replaced: True | expiring within 30d: False | keyUsage: True | key mode: 0o600
valid CA reused (unchanged): True
WARNING:...ca_generator:Cached Root CA at .../holon-root-ca.crt expires within 30 days; regenerating it instead of reusing a stale trust anchor.
```
**C6 entrypoint merged-bundle proof** (real macOS system store `/etc/ssl/cert.pem` as stand-in for `/etc/ssl/certs/ca-certificates.crt`; paths are env-overridable precisely so this is testable):
```
system store certs: 128
--- scenario A: system store present ---
merged certs: 129            # 128 system roots + 1 Holon CA  -> >1 BEGIN CERTIFICATE
bundle mode: 600
SSL_CERT_FILE=/var/.../bundle.crt
NODE_EXTRA_CA_CERTS=/var/.../holon-root-ca.crt   # augments Node, stays on the single-cert mount
--- scenario B: system store missing ---
role_dispatcher: system CA bundle '/nonexistent/ca.pem' is missing or unreadable; building '.../b2.crt' from the Holon Root CA only
merged certs: 1
--- scenario C: bundle unwritable ---
role_dispatcher: could not write merged CA bundle to '.../nodir/b.crt'; unsetting SSL_CERT_FILE/REQUESTS_CA_BUNDLE/CURL_CA_BUNDLE so clients use the image default store (...)
trust-store overrides unset (expected)
```

**Changes**
- `token_reduction/ca_generator.py` — C5 `-addext basicConstraints/keyUsage/subjectKeyIdentifier`, bounded `-days 397`; I10 `-checkend 2592000` + delete-and-regenerate for near-expiry caches (parseable-cache error kept for the unrecoverable case); NIT `_ensure_root_ca()` returns `generated` so `__main__` reports generated vs reused.
- `cli.py` — C6 `SSL_CERT_FILE`/`REQUESTS_CA_BUNDLE`/`CURL_CA_BUNDLE` → `/home/holon/.holon-ca-bundle.crt`, `NODE_EXTRA_CA_CERTS` stays on the RO Holon mount; I11 `_mitm_proxy_ca_paths()` writes `~/.holon/proxy-ca/mitmproxy-ca.pem` (0600, dir 0700) + `mitmproxy-ca-cert.pem` and mounts **only** those two files `:ro` into `/home/mitmproxy/.mitmproxy` (comment states the inherent key requirement and the agent-container exclusion); I12 `NO_PROXY`/`no_proxy`; I13 lowercase `http_proxy`/`https_proxy`; I14 remainder of `run_docker_container` wrapped in `try/finally`; NITs: probe-before-generate in `_attach_external_proxy`, info log before the sidecar `docker run`, `CURL_CA_BUNDLE` comment; `_TOKEN_REDUCE_HELP` marked experimental/not yet functional.
- `entrypoint/role_dispatcher.sh` — C7 `update-ca-certificates` block deleted; merged bundle built as `holon`, `chmod 600`, three vars exported, non-fatal failures logged to stderr (no blanket `>/dev/null 2>&1 || true`).
- `tests/test_token_reduction.py` — I15 crypto-shape tests (keyUsage/CA:TRUE/SKI/checkend), near-expiry regeneration, valid-cache reuse, `SSL_CERT_FILE` never at the single-cert path, `NO_PROXY`/lowercase proxy vars, I11 mount + mode assertions, I14 teardown on early-return/exception/normal paths, probe-before-generate; stdlib patches scoped to the module under test.
- `README.md`, `docs/sandbox/create_plan.md`, `docs/sandbox/execute_plan.md` — I16 experimental/not-yet-functional, merged-bundle trust mechanism, sidecar-only RO key mount, retention/redaction posture (read-only cache, no redaction → locally-owned proxy only); I17 anchor fixed to `#4-optional-token-reduction-proxy---token-reduce`.

⚠️ **ANOMALY (needs your awareness):** while I was editing, a harness-side watcher auto-committed my working tree as `84380d6` with the **stale message** "fix: apply validated PR review suggestions (Iteration 1)" and pushed it to the PR branch at 13:24. My own commit `ffa705f` therefore contains only the last two edits (proxy-ca dir `0700` + its assertion). The pushed tree is complete and verified, but the Iteration-2 content is split across two commits and the first one is mislabeled. I did **not** squash/force-push, because `84380d6` is already published and rewriting it risks racing the same watcher — say the word and I'll `git reset --soft 6e25912 && git commit && git push --force-with-lease` to collapse it into one correctly-labelled commit.
