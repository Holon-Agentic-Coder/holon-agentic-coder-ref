#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export PYTHONPATH="$SCRIPT_DIR/apps/sandbox-executor/src:${PYTHONPATH:-}"
exec python3 -m sandbox_executor.cli "$@"
